import React from "react";
import { View, Text } from "react-native";

const Recipe = () => {
  return (
    <View
      style={{
        flex: 1,
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Text>ReciDDpe</Text>
    </View>
  );
};

export default Recipe;
